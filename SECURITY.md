# Security policy

This project currently reads local JSON files and writes local JSON/SVG output.
It does not provide a network service.

Do not use public issues for vulnerabilities that could cause arbitrary file
access or code execution. Report them privately through the repository
owner's security contact or GitHub's private vulnerability reporting feature.

Only the latest release is expected to receive security fixes. No response-time
commitment is made for this experimental community project.

